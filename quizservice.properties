server.port=8080

# Dynamic database URL for Docker network routing, with local fallback
spring.datasource.url=${QUIZ_DB_URL:jdbc:mysql://localhost:3306/quiz_db}

# Circuit Breaker configuration
spring.cloud.openfeign.circuitbreaker.enabled=true
